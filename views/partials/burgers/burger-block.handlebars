<li>
    <p><span class="label">ID: </span> {{this.id}}<span class="label">Burger:</span> {{this.burger_name}}

        <span><button data-id="{{this.id}}" class="change-devour" data-newdevour="{{devoured}}">
                {{#if devoured}}YUM!{{else}}Devour!{{/if}}
            </button></span>

</li>